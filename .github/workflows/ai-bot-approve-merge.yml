name: AI Bot Approve & Auto-Merge

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, labeled]

permissions:
  contents: read
  pull-requests: write
  checks: read
  statuses: read
  issues: write

concurrency:
  group: ai-bot-approve-merge-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  approve_merge:
    # internal PR only + label gate (ai:in-progress or ai:auto-merge)
    if: >
      github.event.pull_request.head.repo.full_name == github.repository &&
      (contains(github.event.pull_request.labels.*.name, 'ai:in-progress') || contains(github.event.pull_request.labels.*.name, 'ai:auto-merge'))
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

<<<<<<< HEAD
      - name: Approve & enable auto-merge (mizunotarowork)
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.MIZUNOTAROWORK_TOKEN }}
=======
      - name: Approve & enable auto-merge (mizunotarobot)
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.MIZUNOTAROBOT_TOKEN }}
>>>>>>> origin/main
          REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          RUN_ID: ${{ github.run_id }}
        run: |
          pwsh -NoProfile -File .github/scripts/ai-bot-approve-merge.ps1 `
            -Repo $env:REPO `
            -PullNumber $env:PR_NUMBER `
            -RunId $env:RUN_ID `
            -MaxAttempts 10
