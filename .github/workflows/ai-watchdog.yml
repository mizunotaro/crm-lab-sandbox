name: AI Watchdog

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch: {}

permissions:
  actions: write
  issues: write
  pull-requests: read

concurrency:
  group: ai-watchdog-${{ github.repository }}
  cancel-in-progress: true

jobs:
  call:
    uses: mizunotaro/ai-playbook/.github/workflows/reuse-ai-watchdog.yml@ce0d8fc7f92a7bc65e3bdab0a709516cf0ded1ed