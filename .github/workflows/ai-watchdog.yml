name: AI Watchdog

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch: {}

permissions:
  actions: write
  issues: write
  pull-requests: read

concurrency:
  group: ai-watchdog-${{ github.repository }}
  cancel-in-progress: true

jobs:
  call:
    uses: mizunotaro/ai-playbook/.github/workflows/reuse-ai-watchdog.yml@281a4a5f981d608ba87550f1889adf8832392beb

