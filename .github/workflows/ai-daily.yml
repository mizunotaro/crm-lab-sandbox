name: AI Daily Report

on:
  schedule:
    - cron: "0 0 * * *" # JP: UTC 0:00 = JST 9:00 / EN: daily
  workflow_dispatch: {}

permissions:
  issues: write
  pull-requests: read

concurrency:
  group: ai-daily-${{ github.repository }}
  cancel-in-progress: true

jobs:
  call:
    uses: mizunotaro/ai-playbook/.github/workflows/reuse-ai-daily.yml@18822dbf6a08d9f579d344a8cac974e047842ffc


